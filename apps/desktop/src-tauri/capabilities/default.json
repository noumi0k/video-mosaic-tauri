{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Phase 1 desktop shell.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
