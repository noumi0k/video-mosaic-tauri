# アンインストール方針

Phase 1 では、まだ最終 installer は提供していません。

最終 installer で目指している挙動:

- アプリ本体のバイナリを削除する
- 必要に応じて user projects、exports、training data、custom models を残せるようにする
- cache だけの cleanup を選べるようにする
- 完全アンインストールを選べるようにする
